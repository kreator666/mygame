# Assets 资源目录

此目录包含游戏的所有资源文件。

## 目录结构

- `scenes/` - 游戏场景文件（.fire 格式）
- `scripts/` - TypeScript 脚本文件
- `textures/` - 图片纹理资源（可选）
- `audio/` - 音频资源（可选）
- `prefabs/` - 预制体资源（可选）
- `materials/` - 材质资源（可选）
- `models/` - 3D 模型资源（可选）

## 使用说明

在 Cocos Creator 编辑器中，所有资源都会自动导入和管理。只需将资源文件拖放到对应的目录即可。

